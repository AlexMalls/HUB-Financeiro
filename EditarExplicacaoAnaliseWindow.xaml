<Window x:Class="HubFinanceiro.EditarExplicacaoAnaliseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Editar explicação"
        Width="620"
        Height="390"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryColor" Color="#7920DC"/>
        <SolidColorBrush x:Key="PrimaryHoverColor" Color="#8D35EC"/>
        <SolidColorBrush x:Key="BackgroundDark" Color="#1E1E1E"/>

        <Style x:Key="ExplanationTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="11,9"/>
            <Setter Property="Background" Value="#29292E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#494950"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="SelectionBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="TextBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="TextBorder" Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="HubCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#D8D8DE"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="CheckBoxBorder"
                                    Width="18" Height="18"
                                    Background="{StaticResource BackgroundDark}"
                                    BorderBrush="{StaticResource PrimaryColor}"
                                    BorderThickness="2"
                                    CornerRadius="4"
                                    Margin="0,0,8,0">
                                <TextBlock x:Name="CheckMark"
                                           Text="✓"
                                           Foreground="{StaticResource PrimaryColor}"
                                           FontSize="13"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Margin="0,-1,0,0"
                                           Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="MinWidth" Value="105"/>
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource PrimaryHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundDark}"
            BorderBrush="#414148"
            BorderThickness="1"
            CornerRadius="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#242426" CornerRadius="11,11,0,0" MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="Editar explicação"
                               Margin="16,0"
                               VerticalAlignment="Center"
                               Foreground="White"
                               FontWeight="SemiBold"/>
                    <Button HorizontalAlignment="Right"
                            Width="44"
                            Content="×"
                            FontSize="20"
                            Foreground="#BDBDC4"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="Cancelar_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="20,18,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="ClienteText"
                           Grid.Row="0"
                           Foreground="#AFAFB7"
                           FontSize="11"
                           TextTrimming="CharacterEllipsis"/>

                <TextBlock Grid.Row="1"
                           Text="Explicação"
                           Foreground="#D8D8DE"
                           FontSize="12"
                           Margin="0,12,0,6"/>

                <TextBox x:Name="ExplicacaoTextBox"
                         Grid.Row="2"
                         Style="{StaticResource ExplanationTextBoxStyle}"
                         MaxLength="2000"/>

                <CheckBox x:Name="ExplicacaoRecorrenteCheckBox"
                          Grid.Row="3"
                          Style="{StaticResource HubCheckBoxStyle}"
                          Content="Explicação recorrente"
                          HorizontalAlignment="Left"
                          Margin="0,14,0,0"
                          ToolTip="Quando marcada, esta explicação será preenchida automaticamente para o mesmo cliente nas próximas análises."/>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
                    <Button Content="Cancelar"
                            Width="100"
                            Height="36"
                            Margin="0,0,8,0"
                            Background="#343439"
                            Foreground="White"
                            BorderBrush="#505058"
                            BorderThickness="1"
                            Cursor="Hand"
                            IsCancel="True"
                            Click="Cancelar_Click"/>
                    <Button Content="Salvar"
                            Style="{StaticResource PrimaryButtonStyle}"
                            IsDefault="True"
                            Click="Salvar_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
